---
date: 2026-08-08T18:00:00-03:00
author: norte
role: agente
parent: README.md
---
# Transición a repo como fuente de verdad (Norte)

arijd decidió: el Hub se versiona en `git@github.com:nedder3/hub-atlas.git`.
El Desktop ya NO es canal de comunicación; este repo es la fuente de verdad.
Sur lo clonó y dejó el README con el modus operandi. Norte (yo) clona,
configura su firma `user.name=Norte` / `user.email=norte@hermes.local`,
y mueve esta sesión al repo siguiendo la convención de Sur.

## Qué hizo Norte en esta sesión (resumen)
- Instaló pytest + pytest-asyncio en venv Hermes (Mac).
- Escribió andamiaje de tests (conftest.py mocks + test_unit/integration/docs)
  para los RF de Sur, sin funcionalidad (regla de arijd).
- Auditoría de la implementación real de Sur (hub_core.py, transport_mailbox.py):
  APROBADO, 35 passed. Alineó mock-tests al fallback silencioso de A2AClient.
- Hallazgo corregido: A2A nativo NO existe en la versión local de Hermes;
  Sur usó mailbox git-backed (vault como bus compartido). Adoptado.

## Convención adoptada (de README de Sur)
- Commits con scope: `feat(hub):`, `refactor(tests):`, `docs(hub):`, `chore(hub):`.
- Firma Norte para verse como identidad distinta de Sur/arijd.
- Consensos en `HUB/consensos/consens_norte_*.md` con `author:` en frontmatter.
- Push solo tras OK explícito de arijd.

## Pendiente
- Mover handoffs del Desktop al repo (docs/norte/) como evidencia de sesión.
- Esperar OK de arijd para push al remote.
- Definir estrategia de los 3 (arijd + Norte + Sur) — el repo es el canal.
